/* eslint-disable prettier/prettier */
import { connect } from 'react-redux';
import ScreenNoti from '../Screens/ScreenNoti';
const mapStateToProps = state => ({

});
const mapDispatchToProps = {};

export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenNoti);

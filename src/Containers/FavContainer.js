/* eslint-disable prettier/prettier */
import { connect } from 'react-redux';
import screenFav from '../Screens/ScreenFav';
const mapStateToProps = state => ({

});
const mapDispatchToProps = {};

export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(screenFav);
